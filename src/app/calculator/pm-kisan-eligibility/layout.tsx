import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "PM Kisan Eligibility Checker — Check PM-KISAN Samman Nidhi Eligibility",
  description: "Check if you qualify for PM Kisan Samman Nidhi — ₹6,000/year income support for farmer families. Instant eligibility check based on land, income & exclusion criteria.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
