{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "ignoreCommand": "sh -lc 'git diff --name-only HEAD^ HEAD | grep -qvE "^agents/" && exit 1 || exit 0'"
}
